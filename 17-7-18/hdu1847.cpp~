#include <iostream>
#include <stdio.h>
using namespace std;

int main(){
	int n;
	while(~scanf("%d",&n)){
		int l=0;
		while(n){
			if(n&1) l++;
			n>>=1;
		}
		if(l&1) puts("Kiki");
		else puts("Cici");

	}
}
