#include <iostream>
using namespace std;

int pan(int n){
	
}

int main(){
	int T,cas=1;
	scanf("%d",&T);
	while(T--){
		if(cas) 
			if(cas%5==0) puts("");
				else printf(" ");
		scanf("%d",&n);
		for(int i=0;i<n;i++)
			for(int j=0;j<n;j++)
				scanf("%lf",&a[i][j]);
		if(pan(n)) printf("%d",1);
			else printf("%d",0);
	}
}
