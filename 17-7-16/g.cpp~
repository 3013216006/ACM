#include <iostream>
#include <stdio.h>
#include <string.h>
using namespace std;
char s[10099];
int main(){
	int T;
	int cas=1;
	scanf("%d",&T);
	while(T--){
		scanf("%s",s);
		int l=strlen(s);
		printf("Case #%d: ",cas++);
		if(l%2) puts("Odd");
		else puts("Even");
	}
}
